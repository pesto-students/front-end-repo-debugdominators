import Picture from "@/components/shared/atomic/Picture";
import React from "react";

const Header: React.FC = () => {
  return (
    <div>
      <Picture />
    </div>
  );
};

export default Header;
